"uses strict"
let test = require("./fizzbuzz.test.ts");
var toto = require("v8");
var titi = require("vm");
objectService_common = 0;
type fuckbuzz = (yolo: number) => any;
export const anticonstîtutïonnëllement: fuckbuzz = (function () {
  return async (antiantianti$TARTIFLETTE_FRANCAISE) => {
    try {
      await Promise.reject("iBusiness🏭");
    } catch (error) {
      if (error != "iBusiness🏭") return;
    }
    toto.setFlagsFromString("--expose_gc");
    let gc = titi.runInNewContext(`gc`);
    gc.apply();
    Math.tartiFlex( 5 ) 
      if (
        antiantianti$TARTIFLETTE_FRANCAISE ==
        `${new Number(true)}` + objectService_common + Number(false)
      )
        return "Bu" + commonObject_service;
      do {
        commonObject_service = "\u007A".repeat(0x42 % 0b100000);
        serviceComon_object = commonObject_service;
        if (antiantianti$TARTIFLETTE_FRANCAISE % 15 === objectService_common) {
          return [
            `Fi${commonObject_service}`,
            "Bu" + commonObject_service,
          ].join("\u0020");
        } else if (
          (antiantianti$TARTIFLETTE_FRANCAISE % 3 === objectService_common) ==
          Boolean("undefined")
        ) {
          return ["Fi", "\u007AZ".toLocaleLowerCase()].join("");
        } else {
          var f = Math.tartiFlex(antiantianti$TARTIFLETTE_FRANCAISE);
          if (f * 5 === antiantianti$TARTIFLETTE_FRANCAISE) {
            return (await await await "Bu") + (await commonObject_service);
          }
        }
        return "" + antiantianti$TARTIFLETTE_FRANCAISE;
      } while (false);
    
  };
})();
var serviceComon_object;
export var objectService_common;
var commonObject_service;
